# Archives / binaries — keep locally, don't commit
*.zip
Gradient Score info/

# OS / editor
.DS_Store
Thumbs.db
web-deploy/

# Enterprise web preview deploy staging (built artifact — deployed to nowtrendin-v2-preview Heroku app)
web-preview/

# Deploy staging clones (each has its own .git — pushed separately)
.ghpages-deploy/
web-deploy-terminal/

# Python bytecode + local runtime artifacts
__pycache__/
*.pyc
transfer/anomaly_detector.db


HEDGE_FUND_BUYER_ROADMAP_20260809.md.docx
TWO_POLES_TCI_MEDALLION_20260809.md.docx
grifin.pdf

# AB-ATTRIBUTION cohort snapshot (board round 4, item 1a) — 126MB of preserved
# pre-flip inputs. The MANIFEST (counts + sha256) IS committed; the payload is not.
audits/ab-attribution/*.jsonl*
audits/ab-attribution/d_plumbing_ab_rows.json*

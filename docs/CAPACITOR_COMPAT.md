# Capacitor Compatibility Audit — Now TrendIn 2.0
_Generated: 2026-06-11. Read-only audit; no app code was modified._

---

## Findings Table

| Severity | File : Line | Issue | Concrete fix |
|---|---|---|---|
| **HIGH** | `frontend/lib/useGoogleAuth.ts:6-18` | `expo-auth-session/providers/google` + `expo-web-browser` open a popup/redirect inside the Capacitor WebView. Google rejects sign-in from embedded WebViews with `disallowed_useragent` on both Electron (Chromium shell) and Android/iOS WebView wraps. `WebBrowser.maybeCompleteAuthSession()` only patches Expo Go / a standard browser tab; it does nothing in a Capacitor WebView. | (a) Install `@capacitor/browser` in the capacitor shell and swap `promptAsync()` for `CapacitorBrowser.open(authUrl)` + deep-link return via the `nowtrendin://` scheme already declared in `app.json:8`. OR (b) for the desktop Electron shell (which can open a real Chromium window), use Electron's `shell.openExternal(authUrl)` and catch the redirect. Email+password login (`lib/auth.ts:33-36`) and all other API calls work unmodified in all shells — **hide the Google button or show a "not available on desktop" message as the v1 fallback**. |
| **HIGH** | `frontend/lib/storage.ts:9-47` | On web (`Platform.OS === 'web'`), tokens are stored in `window.localStorage`. In Electron, `localStorage` **is persistent** (it lives in the Chromium user-data directory next to the app), so tokens survive restarts — this actually works correctly. However the storage is **unencrypted**; any Electron renderer-privilege exploit can read the raw JWT. In Android/iOS WebView wraps, `localStorage` persistence depends on the WebView configuration and may be cleared on app update. | For v1, `localStorage` is acceptable for Electron desktop. For production harden: use `@capacitor/preferences` (backed by Keychain/Keystore on mobile) instead of localStorage in the Capacitor shells. On Electron, use `safeStorage.encryptString` via an IPC bridge to encrypt tokens before writing. No emergency blocker for a first desktop build, but document the risk. |
| **HIGH** | `frontend/app/_layout.tsx:20-28` | The entire web build is constrained to `maxWidth: 480` centered in a `#D6D9DE` gutter — a phone-width column inside a desktop window. At the suggested Electron default of ~1280 × 800, the app renders as a narrow phone column flanked by 400px of grey on each side. This is functional but looks amateurish for a $250k/yr Enterprise tier desktop product. `Screen.tsx:19` adds a second constraint (`WEB_MAX_WIDTH = 760`) inside the column, further shrinking content. | Change `maxWidth` in `app/_layout.tsx:27` from `480` to at least `900` (or remove the cap entirely and let `Screen.tsx`'s `WEB_MAX_WIDTH = 760` be the readable-column limit). Alternatively add a Capacitor/Electron detection branch (e.g. `window.__CAPACITOR__`) and render the full-bleed layout. Effort: **S** — one constant change. |
| **MED** | `frontend/app.json:8` | `"orientation": "portrait"` is an Expo native-build hint; it does not restrict the Electron window. The app will render in whatever window size/shape the user resizes to. No landscape-aware layout exists; the fixed-column approach masks this on desktop (see above), but if the column is widened, some screens (onboarding carousel `onboarding.tsx:37-38`, horizontal `ScrollView` chips) may look odd at desktop aspect ratios. | Low risk for now (narrow column hides it). Once the column is widened, audit scroll-heavy screens for horizontal overflow. |
| **MED** | `frontend/lib/useGoogleAuth.ts:18` | `WebBrowser.maybeCompleteAuthSession()` is called at module load. In Electron this is a no-op (no browser tab to close), but it attempts to call into `expo-web-browser` native code which is polyfilled on web. It will not crash but emits a silent no-op. Only becomes a problem if `expo-web-browser` is ever replaced with a non-web-polyfilled build. | No immediate fix needed; note this in code comments. |
| **MED** | `frontend/capacitor/capacitor.config.json` (root) | `"webDir": "www"` but the Expo web export writes to `frontend/dist`. The `setup-capacitor.ps1` script must copy/rename `dist` → `www` before `cap sync`. If this copy step is ever skipped, the shell loads a stale or empty bundle. | Add an assertion in `setup-capacitor.ps1` that `capacitor/www/index.html` exists after the copy and abort with a clear message if not. Low effort. |
| **MED** | `frontend/app/(auth)/onboarding.tsx:37-38` | Uses `useWindowDimensions()` and caps `width = Math.min(winWidth, 480)` for the paged carousel. In Electron at 1280px, `winWidth` will be ~1280, so the cap kicks in at 480. The `scrollTo({ x: ni * width })` calculation references the capped `width`. This is actually correct behaviour — but if the `_layout.tsx` column is widened (see HIGH above), the cap here must be updated in sync or the carousel will mis-scroll. | Keep in sync with `_layout.tsx` max-width change. |
| **MED** | `frontend/components/ui/Screen.tsx:43` | `SafeAreaView edges={['top','left','right']}` always applied, including on web/Electron. Electron has no notch/status-bar, so this adds unnecessary safe-area padding (typically 24–44px from the top). On `react-native-safe-area-context` web, safe area insets default to 0, so in practice there is no visible padding — but it depends on `SafeAreaProvider` correctly reporting 0 insets in Electron. Verify at runtime. | Add `if (isWeb) return <View className="flex-1 bg-bg">{children}</View>` early return in `Screen`, bypassing `SafeAreaView` on web/Electron. Effort: **S**. |
| **MED** | `frontend/lib/storage.ts:12-13` | `typeof window !== 'undefined'` guard is correct for SSR safety, but Electron's renderer process always has `window`, so the guard is always true. No issue, but document this explicitly so a future developer doesn't add a `window` check thinking it guards against Electron. | Comment only. |
| **LOW** | `frontend/app.json:24-27` | `"web": { "output": "single" }` produces a single `index.html` with all JS bundled — correct for a Capacitor SPA shell. The Capacitor local server (`capacitor://localhost`) serves this file directly; expo-router's client-side routing will handle all navigation. Cold-starting into a deep route (e.g., `capacitor://localhost/(app)/signal/foo`) via an OS deep link would 404 if the local server tried to serve a file at that path. However, since all navigation in the app is client-side (no direct URL entry by users), this is low risk for v1. The `nowtrendin://` deep link scheme is also declared (`app.json:8`), which Capacitor can intercept for OAuth returns. | For robustness, confirm `capacitor.config.json` does not set a custom `server.url` that would bypass local-file serving. Currently it does not — only `androidScheme: "https"` is set. No change needed for v1. |
| **LOW** | `frontend/lib/auth.ts` (all), `frontend/lib/api.ts:6-7` | `BASE_URL` falls back to `https://nowtrendin-backend-acb79c396814.herokuapp.com` if `EXPO_PUBLIC_API_URL` is unset. This is an absolute HTTPS URL — correct for all Capacitor origins (`capacitor://localhost`, `https://localhost`). CORS on the backend is already `CORS_ALLOW_ALL_ORIGINS=True` per `PLATFORMS.md`. No issue. | None. Document that `EXPO_PUBLIC_API_URL` and `EXPO_PUBLIC_GRADIENT_API` must be set at `expo export` time if pointing to a non-Heroku backend. Both have safe Heroku defaults. |
| **LOW** | `frontend/lib/gradientApi.ts:6` | `GRADIENT_API` similarly falls back to `https://nowtrendin-e62dcb9ecb69.herokuapp.com` — absolute HTTPS, no issue. | None. |
| **LOW** | `frontend/app/(app)/alerts.tsx:79` | Uses emoji `🔔` inline in a `<Text>` component. Renders fine on modern Electron (Chromium), Android, and iOS. Not a compat issue. | None. |
| **LOW** | `frontend/components/ui/Screen.tsx:2,44` | `StatusBar` from `react-native` is imported and rendered on every screen. On web/Electron the React Native Web polyfill for `StatusBar` is a no-op, so this neither crashes nor does anything visible. | None. |
| **LOW** | Stripe / expo-notifications | `@stripe/stripe-react-native` and `expo-notifications` are **not imported anywhere** in the web bundle paths (confirmed by grep — no matches in `frontend/**/*.{ts,tsx}`). These are correctly deferred per CLAUDE.md. No crash risk. | None. |
| **LOW** | `frontend/app/(app)/_layout.tsx:16-19` | Tab bar `height: 60, paddingBottom: 8, paddingTop: 6` is hardcoded. On desktop Electron this is fine — the tab bar will appear at the bottom of the 480px column. If the column is widened to a full desktop layout, consider a left-sidebar nav instead of a bottom tab bar. | Cosmetic improvement for a proper desktop layout in a future iteration. |
| **LOW** | `frontend/app/(auth)/splash.tsx:39` | `SecureStore.getItemAsync('access_token')` on startup. Reads from `localStorage` in Electron — this is synchronous under the hood but wrapped in a Promise. No issue; just confirm the Electron renderer allows synchronous localStorage access (it does by default). | None. |
| **LOW** | No `window.location`, `history.pushState`, or `document.*` calls found anywhere outside `lib/storage.ts`'s `window.localStorage` usage. No `navigator.*` usage (camera, geolocation, share, clipboard) found. | None. |

---

## Prioritized Fix List for Capacitor Desktop v1

> Effort: **S** = <1 hour, **M** = 2–4 hours, **L** = 1–2 days.

| Priority | Fix | Effort | Blocks release? |
|---|---|---|---|
| 1 | **Google OAuth** — disable/hide the "Continue with Google" button in Capacitor shells (or show "not available on desktop") as the v1 stopgap. Implement the system-browser flow (`@capacitor/browser` + deep-link) in v2. | S (disable) / L (full fix) | YES — if left as-is, Google login silently fails or throws `disallowed_useragent`. Email+password works fine. |
| 2 | **Layout column width** — change `maxWidth` in `app/_layout.tsx:27` from `480` to `900` (or add a Capacitor detection branch). Update `onboarding.tsx:38` cap to match. Default Electron window: `1280 × 800`. | S | No (functional but looks bad at 480px in a 1280px window) |
| 3 | **`webDir` copy validation** — add assertion in `setup-capacitor.ps1` that `capacitor/www/index.html` exists after the copy step. | S | No (operational safety) |
| 4 | **SafeAreaView on Electron** — add early return in `Screen.tsx` bypassing `SafeAreaView` on web (`Platform.OS === 'web'`). Prevents any surprise padding if `react-native-safe-area-context` ever returns non-zero insets in the Electron shell. | S | No |
| 5 | **Token storage hardening** — for v1 `localStorage` is fine. Before Enterprise customer rollout, add `@capacitor/preferences` (mobile WebView) and an Electron IPC encrypted storage bridge. | L | No for v1 |
| 6 | **Full Google OAuth with system browser** — implement `@capacitor/browser` + `nowtrendin://` deep-link return, update Google Cloud Console with the `com.nowtrendin.app` redirect URI. | L | No for v1 (email+password covers it) |

---

## Notes

- **No `window.location` / `document.*` / `navigator.*` abuse found** anywhere in the app (other than `window.localStorage` in `lib/storage.ts`, which is correct and intentional).
- **No Stripe or expo-notifications code exists in the web bundle** — confirmed by source grep.
- **Both API base URLs are absolute HTTPS** with Heroku defaults; no relative URL fetches.
- **expo-router SPA mode** (`web.output: "single"`) is correct for Capacitor local serving; all routing is client-side.
- **CORS is pre-configured** on both Django and FastAPI backends for Capacitor origins — no backend changes needed.
- The `app.json` `scheme: "nowtrendin"` deep-link is already declared — this can be used for the OAuth redirect return without new configuration.

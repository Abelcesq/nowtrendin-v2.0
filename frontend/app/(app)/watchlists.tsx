// Toolbar-level route alias for the Watchlists screen, so a member can pin
// "Watchlist" to a bottom-toolbar slot (Edit Toolbar Icons). Same screen as
// /profile/watchlists — re-exported to avoid duplicating the component.
export { default } from './profile/watchlists';

// Toolbar-level route alias for the Favorites screen, so a member can pin
// "Favorites" to a bottom-toolbar slot (Edit Toolbar Icons). Same screen as
// /profile/favorites — re-exported to avoid duplicating the component.
export { default } from './profile/favorites';

{
  "name": "nowtrendin-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell wrapping the Expo web build (frontend/dist) into installable apps — desktop via Electron, plus optional iOS/Android WebView wraps. The store mobile apps remain the Expo native build.",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  }
}

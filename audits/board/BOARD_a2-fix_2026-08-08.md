# BOARD REVIEW — A2 fix (issuer-page adapters + two harness defects) — 2026-08-08

Six independent archetype memos on `A2_FIX_WORKUP_2026-08-08.md`. Identical evidence
pack (workup · A2 amendment · §16 survey · full diff b0a2df0..53253d8 · both
implementation files · live production verification record); no seat saw another's
output. This document is a COLLATION — each memo condensed faithfully, disagreements
explicit, verdict table at the end. No operator recommendation is included.
Items: **A** = issuer adapters + FMP demotion · **B** = same-second PK-collision fix ·
**C** = NO_DERIVED era-attribution fix · **Q1–Q5** = the workup §6 open questions.

---

## 1. THE CHALLENGER — A: APPROVE-W/C · B: APPROVE-W/C · C: APPROVE-W/C

**A.** Strongest attack: fail-closed protects against NON-parse, not MIS-parse — a
site redesign can repoint a regex at a plausibly-sized wrong number and nothing
catches it; the pages themselves offer the free self-consistency check
**shares × NAV ≈ AUM** and the adapters don't use it. Second: the workup's "nothing
frozen" claim is **contradicted by its own record** — XRPC `page_asof 2026-07-31` was
~9 days stale at capture, the exact frozen-value class FMP was fired for, and XRPC
has no comparator so nothing will ever police it. Third: the FMP "silent 30-day
comparison" has **no pre-declared metric or pass/drop criteria** for the 2026-09-05
re-eval — declare them before the data exists. Minor: the workup states both 19/19
and 22/22 for the same suite (evidence records shouldn't carry two numbers for one
fact). Conditions: identity check fail-closed on violation; correct the frozen claim
+ investigate XRPC staleness; pre-declare the FMP comparison rule; archive raw
fetched payloads (or hashes+snippets) per capture.

**B.** Fix works, but treats the symptom: the PK still lacks `src` and
`ON CONFLICT DO NOTHING` still swallows any future collision invisibly — correctness
rests on timing luck, not on the key identifying the row. Condition: add src to the
key or a surfaced conflict counter.

**C.** States plainly what the workup soft-pedals: the change **moved a gate-driving
figure (open_bad 12→0)** — maximum scrutiny applied — and it survives: the 12 rows
predate the issuer's first strike (structurally uncoverable), A2.4 pre-declared "no
FMP-era row counts," the rule is deterministic with no per-row freedom,
regression-tested, before-state preserved, and the pre-fix behavior would have
blocked re-arm PERMANENTLY (an unsatisfiable gate is a broken instrument, not
rigor). Three surviving attacks: (1) **NEW DEFECT FOUND — the value-distinct strike
dedupe is src-blind** (`_strikes_with_capture` compares shares+NAV only): if the
first issuer strike equals the last FMP strike to 1e-9, the seam is swallowed, the
issuer era starts LATER, and issuer-era bad days get blamed on FMP — a
pro-readiness bias the process would never self-report. Make dedupe src-aware
(one line + regression). (2) Semantic changes under an unchanged rule_version leave
no in-database trace — adopt a written rule: bump a sub-version or archive
pre-change rows verbatim. (3) The NO_DERIVED materiality floor uses TODAY's AUM for
July days (pre-existing, now visible — note it before someone else finds it).

**Q1** migrate iShares to the official endpoint; browser-UA only as documented
interim with payload archiving. **Q2** reading confirmed in code; wave-3 HIGH; flip
condition: Fidelity wired OR covered-AUM disclosure with FBTC declared absent.
**Q3** REJECT strike ingestion outright; separate held-out table if research value;
resolve XRPC staleness first. **Q4** pre-declare the full epoch break now (ratio,
epoch id, seam-not-failure semantics, t5 regression before 10-06, no
ratio-adjustment of history). **Q5** per-family rows NOW — with one row, the entire
iShares family incl. IBIT can die silently (7≥5).

---

## 2. FIRST-PRINCIPLES GUARDIAN — A: APPROVE-W/C · B: APPROVE · C: APPROVE-W/C

**A.** Moving from a vendor recomputing the fact to the originating source is
movement TOWARD first principles. Verified line-by-line: fail-closed, provenance,
splice, no backfill, nothing touches a served score. Conditions: **A-1** — post-swap,
BOTH legs trace to the issuers (derived = issuer shares×NAV; comparator = Farside
aggregating issuer-published flows), so a PASS is issuer-vs-issuer
self-consistency; the A2.5 wording rule ("pipeline-fidelity evidence, not
independent confirmation") must appear in EVERY future characterization of re-arm
evidence, permanently. **A-2** — fail-closed catches a page that stops parsing, not
one serving a FROZEN value; for comparator-less funds (XRPC/TOXR) that mode is
invisible forever, and XRPC is already 8+ days stale ("an eyeball is not a
control") — add a page_asof staleness warning. **A-3** — the disqualified source
still writes scored strikes for uncovered funds; defensible pre-flip, a live
contradiction at flip (see Q2).

**B.** APPROVE clean — no silent data loss; caught pre-deploy; verify-before-ship
honored in fact, not narration.

**C.** Scrutinized hardest because its direction of effect (removing flip
obstacles) is where self-serving rationalization hides. Honors the pre-declared
standard: A2.4 itself condemned the old behavior; mechanism-derived with zero
per-row freedom; strict-inequality boundary is structurally forced, not lenient;
nothing deleted (the 13 failures still show, correctly on FMP); ruling (a) covers
recomputation. Conditions: **C-1** — the pre-fix state survives only as a summary
line, a LOWER evidentiary bar than the A1.5 frozen-log standard set three days
earlier; archive the pre-fix pass output (and pre-fix row stamps if recoverable)
verbatim into audits/board/. **C-2** — the era rule lives in code comments, not the
governing spec; append a dated annex to the A2 amendment. **C-3** — make both
standing practice for ANY within-rule_version semantic change. Named drift risk:
the development-phase statement is a solvent that works only while the mechanics
keep doing the actual work.

**Q1** interim-acceptable (honest token, public document, passive wall — and the
build correctly did NOT escalate against Grayscale's ACTIVE wall; preserve that
line as doctrine); migrate iShares to the official endpoint in a bounded window.
**Q2** reading confirmed; before flip, resolve in writing whether the served leg
consumes FMP-only funds — else the flip arms a leg partially fed by the source this
amendment disqualified; Fidelity HIGH (derived-precise path is itself
issuer-official). **Q3** REJECT for strikes, categorically (backfilled rows have no
honest capture instant = fabricated data); held-out reference table permissible
with same-issuer-both-legs disclosure. **Q4** pre-declare now: epoch mechanics,
issuer's announcement as ratio authority, NO retroactive ratio-adjustment ever,
expected artifact class declared in advance, discontinuity guard satisfied via the
seam only (threshold not widened); own spec id/annex before October. **Q5**
per-family NOW — the registry's own comments record the lesson; a whole-family
silent death is not hypothetical (Grayscale today).

**Closing:** the vision recognizes this work — it repeatedly chose the gap over the
guess. The one shortfall vs its own standard is C-1, repairable this week.

---

## 3. THE EXPANSIONIST — A: APPROVE-W/C · B: APPROVE · C: APPROVE

**A.** Issuer-direct primary + vendor-demotion-with-dated-re-eval is the most
globally sellable decision in the set and a reusable vendor-management template;
loud failure economics are correct (silent staleness at 100× is a lawsuit).
Conditions: (1) codify a source-preference ORDER as policy — official data endpoint
→ server-rendered JSON → rendered-HTML regex as last resort with a migration ticket
— and migrate iShares to the fund-download endpoint first in wave 3 (regex over
marketing HTML breaks on non-English pages and marketing redesigns); (2) **record
the locale hard-code**: the A2.1 t_of mapping is US-only by construction (US
Eastern, US trading days) — correct today, but write the §16a-style boundary
sentence NOW: no non-US-listed fund enters this universe until the join rule is
parameterized per listing venue; (3) per-family health rows now (promoted to a
condition). Opportunity: the issuer-direct provenance layer is the template for an
issuer-direct source tier across every asset class and region.

**B.** APPROVE as shipped; schedule the structural fix — add src to the
observation uniqueness key before the per-fund writer count grows (two sources
observing the same fund are distinct facts by schema, not by timestamp jitter).

**C.** The best engineering in the set: not a bug fix but **the general algorithm
for source cutovers**. Era attribution + splice + provenance = a
jurisdiction-agnostic source-migration framework — a precondition for operating on
every continent. Pure opportunity.

**Q1** interim-accept; fund-download migration first wave-3 task; adopt the
preference-order policy so this question never recurs per-issuer. **Q2** reading
confirmed from code (and it only holds BECAUSE of item C); wave-3 high; §17
covered-roster disclosure mandatory whenever the leg ships. **Q3** general
held-out-history rule or skip entirely — a bespoke XRPC exception is parochial
machinery. **Q4** pre-declare the MECHANISM: a standing scheduled-series-break
table (ticker, effective_date, new_src_epoch, reason, declared_at), ETHA row #1;
epoch scoped to the FUND (IBIT unseamed); ratio recorded, never used to synthesize
continuity; plan re-arm evidence around ETHA's clock reset. **Q5** per-family NOW —
"not close"; min_distinct=5 lets Canary die or 21Shares lose 2 of 3 silently;
suggested rows: ishares 2 · bitwise 3 · 21shares 3 · canary 1.

---

## 4. THE OUTSIDER (VC / banker, first look) — A: APPROVE-W/C · B: APPROVE · C: APPROVE-W/C

One-sentence read for partners: "an attention-and-money-flow terminal that keeps
its crypto-ETF flow signal switched OFF until its computed flows reconcile, under a
pre-declared tolerance, with the numbers the fund issuers themselves publish."
**Managed-numbers check: NOT managed** — after a full night of fixes the system
still reports gate FAIL / ready false, and nobody edited that answer; every defect
found HURT the gate. Portfolio-level caution: the measuring stick has now been
re-defined several times (A1→A2 join, enrollment, blended→tracked-race,
latest→era blame), each defensible and pre-declared, but the cumulative direction
is one-way — keep publishing old-next-to-new, forever. **Jargon audit:** "re-arm,
strike, era attribution, NO_DERIVED, seam, open_bad…" — internally fine, externally
fatal; produce a one-page client glossary before any external exposure.

**A.** Conditions: silent family death behind one health row (see Q5), and coverage
honesty — the wired set excludes Fidelity and all of Grayscale; nothing
client-facing may say "the BTC ETF complex" until covered-AUM share is computed and
disclosed. Point-blank: *"What percentage of total BTC and ETH ETF assets does your
nine-fund set represent — and are you comfortable printing it on the product
page?"*

**B.** APPROVE. Point-blank: *"Why isn't the key (ticker, captured_at, src), and
have you audited every other shared table for the same collision class?"*

**C.** Honest bookkeeping, checked as a hostile LP would: the days predate the
source's existence (an F for a semester before enrollment); the standard was
pre-declared; no FAIL became a PASS and the light stayed red; before-state
preserved; rule deterministic. Conditions: (a) write the era rule into the spec as
a formal A2 clarification; (b) **close the cold-start corner** — as coded, a fund
whose FIRST era is issuer-sourced will have pre-first-strike published days blamed
on the ISSUER (same structurally-uncoverable-day defect, one universe over); fix
before the first issuer-native fund onboards. Point-blank: *"Every defect you've
caught hurt your gate. What process hunts the defects that HELP it, and when did
that hunt last run?"*

**Q1** NOT acceptable permanent posture: a Chrome-lookalike UA past a wall the
publisher built is a ToS conversation waiting to happen — build the official
endpoint as primary, keep browser-UA as documented env-flagged fallback, have
counsel eyeball the site terms once, before the flip. **Q2** reading correct; the
substance is a disclosure problem; Fidelity urgent — and make the 2026-09-05
FMP-drop decision conditional on Fidelity coverage existing (FMP is currently the
only thing watching FBTC at all). **Q3** no strikes; separate labeled reference
table as validation referee; low urgency. **Q4** pre-declare everything now incl.
expected ratio so the discontinuity guard can VERIFY rather than alarm; "the
cheapest credibility purchase on the table." **Q5** per-family now — "a rule you
defer until it's needed was never a rule."

---

## 5. THE EXECUTIONER — A: SHIP (ratified, 4 conditions) · B: SHIP · C: SHIP

Code-verified preliminaries: era logic matches the described mechanism; upsert PK
confirms in-place restamp per ruling (a); microsecond stamp is issuer-writer-only
and t_of parses both; FMP observations still land (no history destroyed); re_arm
computed on non-FMP rows only; health row 360m vs 240m cadence satisfies the
stale-window rule.

**A.** Four conditions: (1) **two-flag dark corner** — `ETF_ISSUER_PAGES=0` +
`ETF_ISSUER_PRIMARY=1` leaves NO writer owning covered strikes (nine funds dark,
surfacing only via slow NO_DERIVED); same hazard from the silent import-fallback
that re-promotes FMP; couple the flags or document "flip BOTH or neither" + alarm
the fallback. (2) **page_asof staleness guard** — extend fail-closed to declared
absence when page_asof older than N trading days (N=3 generous); the live record
itself shows XRPC nine days stale. (3) **monitor keying** — gate_status stays FAIL
for weeks on dead FMP rows (they age out ~late August); verify `etf_reconcile_watch`
keys on `open_failures_live_source` + `re_arm`, not raw gate, or it cries wolf for a
month. (4) per-family health rows (Q5).
**Verify (48h):** two cycles → issuer observations + zero new FMP strikes on
covered tickers (2 obs/ticker/cycle: one fmp, one issuer); takeovers → 0 after day
one; re_arm counters move as PENDING_FINAL converts; missing stays [].
**Rollback:** unset BOTH flags → FMP resumes; the return seam is the already-tested
splice class; issuer rows harmless at rest. Genuinely reversible.

**B.** SHIP. Codify in the module docstring: every future observation writer stamps
microsecond captured_at.

**C.** SHIP — the only implementation consistent with A2.4; symmetric (a frozen
issuer source blames the issuer and blocks re-arm — failure never silence, both
directions). Note: commit the evidence pack to audits/board/ so 0→12→0 is permanent
record, not scratchpad. Rollback is "revert + reopen the defect" — i.e., none
desirable; document as such.

**Q1** ACCEPT interim; SHIP-LATER the download-endpoint as primary parse path. Draw
the doctrine line now: declared-UA on a public page acceptable; headless/session
escalation past an active wall (Grayscale 429) crosses to circumvention — needs its
own board ruling. **Q2** reading CONFIRMED mechanically; the hidden question is the
FLIP: before it, either Fidelity derived-precise is live or FBTC/FETH are excluded
from the served leg with a declared coverage statement (the §17 defect inverted).
**Q3** CUT this wave — zero gate value, nonzero integrity surface; XRPC's real
problem is the stale page. **Q4** pre-declare now with a spec id; epoch
`issuer_ishares_r1` (ETHA only), existing splice machinery handles it with one
stamp; shares×NAV ~continuous across the split as the pre-declared sanity check;
**deadman guard: if the epoch stamp + t5 regression are not deployed by 2026-09-29,
the ETHA adapter auto-disables** rather than ingest the cliff. **Q5** per-family
NOW; suggested min_distinct: ishares 2 · bitwise 3 · 21shares 3 · canary 1; do it
this week.

**Ship order:** (1) now — monitor-keying check, commit evidence pack; (2) this week
— family health rows + staleness guard + flag coupling; (3) two weeks — ETHA
pre-declaration + code + t5 + deadman; (4) before flip (hard) — Fidelity adapter OR
declared exclusion; calendar the FMP re-eval; (5) later — iShares endpoint
migration; Grayscale/VanEck only with a legitimate-access path; (6) cut — XRPC
backfill, headless circumvention, any use of as-of stamps in t_of.

---

## 6. THE ECONOMIST — A: APPROVE-W/C · B: APPROVE · C: APPROVE

**A.** (Smith; Friedman & Schwartz.) FMP's shares field was a distorted price
(AUM÷NAV from desynced vendor fields — fabricating flow and freezing through real
inflow); when the intermediary's posted price is distorted, go to the primary
market. The live IBIT delta (1,304.65M frozen vs 1,318.0M issuer) is the primary
source carrying information the intermediary destroyed. Keeping FMP running
silently in parallel is F&S discipline: run the distrusted series alongside and let
the divergence record settle the 09-05 argument. Conditions: the one-day eyeball is
n=1 (the A2.4 accumulation IS the validation — confirmed nothing shortcuts it);
migrate iShares to the designed distribution channel; per-family health (single row
can read HEALTHY with two entire families dead: 3+3=6≥5); the FBTC hole is the
largest open risk — the worst FMP offender is the one fund still on FMP strikes.

**B.** (Taleb — silent evidence.) A record that silently drops rows is worse than
one that errors; caught pre-production by the system's own test discipline. Noted
for prescriptions: DO NOTHING remains the conflict posture; a cheap swallowed-
conflict counter converts any recurrence from silence into evidence.

**C.** (F&S tested against Reinhart & Rogoff.) The four-question revision test: did
data change or attribution? (attribution only — no observation, verdict, floor, or
band moved); fitted to outcome or derived from mechanism? (mechanism — the old
"blame the latest src" was the fitted-to-convenience rule); symmetric? (yes — it
will blame the issuer era just as mechanically, and the 5 PENDING_FINAL rows show
the harness refusing to credit the issuer early); prior record preserved? (yes —
0→12→0 verbatim in the pack, t4 pins it). Decisive check: **the fix did not advance
the outcome the builder wants** — after it, ready still reads false; it removed a
permanent false block, and an unsatisfiable gate is a broken instrument, not rigor
(Bernstein). Honest bookkeeping. Standing condition → prescription 5.

**Q1** interim acceptable (reading the posted price with an honest token is not
deception); the official endpoint is the designed tap — migrate on wave-3 timing;
fragility, not blocker. **Q2** reading confirmed; but "does not block re-arm" ≠
"does not matter" (Taleb: the validation sample excludes the fund where the errors
historically lived); flip must state one of: (a) wave 3 complete; (b) FBTC/FETH
excluded from the served leg with coverage disclosed as % of BTC ETF AUM at issuer
grade; (c) explicit Chairman acceptance of the FMP residue on the record. Silence
is the only unacceptable option. **Q3** held-out research archive ONLY (assembled
history labeled as assembled, never spliced into live-captured data; backfilled
rows cannot pass t_of honestly; can never count toward re-arm regardless — rows we
did not capture prove nothing about our pipeline). **Q4** pre-declare now (R&R):
epoch id effective at first post-10-06 capture; official ratio with a pre-declared
RATIO CONTINUITY CHECK (post-split shares × ratio ≈ pre-split, a falsifiable
prediction made in advance); boundary interval VOID/PENDING never FAIL; guard
whitelisted for that ticker+date only; ETHA's Δ-clock restarts but the SOURCE's
standing does not (the instrument re-denominated; the source didn't fail).
Generalize: a per-fund SCHEDULED-EVENTS CALENDAR (ETHE distributions, staking
funds), ETHA entry #1. **Q5** per-family NOW (Bernstein: an aggregated health row
is measurement that manufactures residual risk; iShares+Canary can both be dark at
6≥5).

**PRESCRIPTIONS** (verbatim-condensed):
1. **Null-baseline comparator on the flow leg** (Malkiel): record alongside re_arm
   what a naive predictor (trailing-sign; always-inflow) scores on the SAME
   intervals under the SAME band — one number, zero gate effect, permanent immunity
   to "our harness passed" narrative; extend to the attention ledger (publish the
   naive baseline's hit rate next to ours, always).
2. **Tail-weighted standing report** (Taleb): post-flip, split harness performance
   on top-decile |published flow| days vs the rest; same split on the attention
   ledger. A track record on $12M days is silent about the $500M day, and the $500M
   day is the product.
3. **Coverage-weighted honesty metric**: publish in /diag/etf-reconcile the % of
   each coin's ETF AUM whose strike source is issuer-grade vs fmp vs absent; the
   flip record should read "covers X% of BTC ETF AUM at issuer grade." Generalize:
   every served composite carries a machine-computed coverage statement.
4. **Persist the source ledger** (F&S): the eras structure (src runs, first/last
   strike, seams, epochs per fund) is this instrument's Monetary-History appendix —
   persist it as a table or generated audit doc.
5. **Standing rule: archive the pre-fix reading** (Zweig/Belsky): any fix changing
   a gate-relevant output archives the pre-fix report verbatim in audits/ before
   deploy — the cheapest defense against the builder's own hindsight bias.
6. **Audit the "latest-wins" attribution class** (Bernstein): sweep for provenance
   fields stamped from CURRENT state rather than the state that produced the row
   (src, tier, category, maturity at record-time); make it a standing monitoring
   check.
7. **Scheduled-event calendar with pre-declared handling** (R&R): known future
   discontinuities handled ad hoc on the day is how "this time is different" enters
   a clean series; the ETHA split is visible 59 days out.
8. **The monetary conditions of attention** (F&S, Kindleberger): once flipped, log
   held-out the LEAD/LAG ordering between attention-gradient detection and the flow
   leg per coin — flows lagging attention = confirmation instrument (label it so);
   flows leading = displacement-stage information. Either answer is valuable;
   assuming the answer is the only error.

---

## DISAGREEMENTS (signal, not noise)

1. **Item C severity.** Economist + Expansionist + Executioner: clean
   APPROVE/SHIP. Guardian + Challenger + Outsider: APPROVE-WITH-CONDITIONS —
   conditions differ: Guardian wants row-level pre-fix archival (C-1) + a spec
   annex (C-2); Challenger wants the src-blind dedupe fixed + a written
   sub-versioning rule + the 12→0 stated plainly; Outsider wants the spec annex +
   the cold-start corner closed. No seat found the fix illegitimate.
2. **Q1 UA posture — the sharpest split.** Outsider: NOT acceptable as permanent,
   counsel review + endpoint migration BEFORE the flip. Challenger: interim only
   WITH raw-payload archiving. Guardian/Executioner/Expansionist/Economist:
   acceptable interim, migrate on wave-3 timing; Guardian + Executioner both draw
   the same doctrine line (declared-UA on a public page = ceiling; escalation past
   an active wall needs its own ruling).
3. **Q3 XRPC backfill.** Unanimous: never into the strike series. Split on the
   alternative: Executioner CUT entirely (not worth the surface); Expansionist
   general-rule-or-skip; Guardian/Challenger/Outsider/Economist permit a separate
   held-out reference table under written rules.
4. **Q2 flip condition phrasing.** All six confirm the A2.4 reading and rate
   Fidelity HIGH; they differ on the flip requirement: Guardian/Executioner/
   Economist make it a hard either/or (adapter live OR served-leg exclusion/
   disclosure; Economist adds explicit-Chairman-acceptance as a third path);
   Outsider uniquely ties the 2026-09-05 FMP-drop decision to Fidelity coverage
   existing (FMP is currently the only watcher on FBTC).
5. **Unique findings no other seat surfaced.** Challenger: the src-blind strike
   dedupe (a live pro-readiness bias candidate) · XRPC staleness contradicting the
   workup's "nothing frozen" · no pre-declared FMP-comparison metric · the
   19/19-vs-22/22 inconsistency. Outsider: the issuer-native cold-start corner in
   era attribution. Executioner: the two-flag dark corner · monitor keying on raw
   gate. Expansionist: the US-trading-calendar locale boundary on t_of.

## VERDICT TABLE

| Item | Challenger | Guardian | Expansionist | Outsider | Executioner | Economist |
|---|---|---|---|---|---|---|
| A adapters+demotion | APPROVE-W/C | APPROVE-W/C | APPROVE-W/C | APPROVE-W/C | SHIP (4 cond.) | APPROVE-W/C |
| B PK-collision fix | APPROVE-W/C | APPROVE | APPROVE | APPROVE | SHIP | APPROVE |
| C era attribution | APPROVE-W/C | APPROVE-W/C | APPROVE | APPROVE-W/C | SHIP | APPROVE |
| Q1 UA posture | interim + archive payloads | interim, bounded migration | interim, policy order | NOT permanent; counsel + endpoint pre-flip | interim; endpoint SHIP-LATER; doctrine line | interim; designed tap on wave 3 |
| Q2 FBTC/wave-3 | confirmed; HIGH; flip disclosure | confirmed; resolve served-leg in writing pre-flip | confirmed; §17 roster disclosure | confirmed; tie FMP-drop to Fidelity | confirmed; hard flip precondition | confirmed; (a)/(b)/(c), silence unacceptable |
| Q3 XRPC backfill | reject; held-out table maybe | reject strikes; held-out OK | general rule or skip | no strikes; referee table | CUT this wave | held-out research archive only |
| Q4 ETHA split | pre-declare now + t5 | pre-declare now, own annex | standing series-break table | pre-declare now + ratio | pre-declare + 09-29 deadman | pre-declare + continuity check + event calendar |
| Q5 health rows | per-family NOW | per-family NOW | per-family NOW | per-family NOW | per-family NOW (this week) | per-family NOW |

**Unanimous:** every seat approves/ratifies all three items (with conditions as
listed); Q5 per-family health rows NOW (6/6); Q4 pre-declare the ETHA break now
(6/6); Q3 never into the strike series (6/6); Q2 A2.4 reading confirmed + Fidelity
HIGH (6/6). Nothing reviewed re-arms or flips anything.

**Chairman — your decision per item.**

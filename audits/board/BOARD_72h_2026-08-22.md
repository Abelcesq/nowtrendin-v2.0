# BOARD ROUND 6 — 72-HOUR REVIEW, COLLATION FOR THE CHAIRMAN
### Nine seats, 2026-08-22. Question: did the fixes address your prior concerns, and what next?
### COLLATION, not a blend. Disagreements preserved. No recommendation.

---

## 1. THE VERDICT, AND IT IS NOT CLOSE

**Nine seats. The class was NOT fixed. It recurred THREE MORE TIMES inside the round convened
to kill it — twice inside fixes made in the same 72 hours, once inside a fix made the same hour.**

**Source review is now 0-for-6. Every defect in six rounds was found by EXPERIMENT.**

| What the pack claimed | What nine seats found |
|---|---|
| "FIXED, with a negative control proven to fire" (16 rows) | **8 of 16 rows have no control at all**; one cites a test file that does not exist (Challenger) |
| "Gate 19/19, Suite 13 passed" | **The suite was RED at that commit.** `fe6712b` shipped `REFLEXIVITY_AUDIENCE_TRIP = 5001` — a board agent's live mutation swept up by `git add -A` — and the failing test was the named enforcer of `C-R2-REFLEXIVITY` |
| "corroboration_at_enroll DISABLED" | **The file was not in the commit.** A different agent's restore overwrote the edit; the stamp was still live |
| "the arithmetic that did not change" | **It changed and moved DOWN**: 58→61 races, 15→15 LED, 25.9%→24.6%. Three resolved in-window, zero were wins |
| ">90% of topics" in the NULL stratum | **14.5% at the top of the feed, 63–70% deep.** Fifth load-bearing figure inflated in the author's favour |
| 4c fixed | **1 of 4 paths.** `_format_score_rows`, `calibration_engine:1015`, `Screener.tsx` all still fall through on NULL |
| 11 round-4 rulings, "1a+1b done, eight open" | **Eleven open. One genuinely done (ruling 8).** The ledger miscounts its own open set by three |

---

## 2. THE FINDING THAT OUTRANKS THE ENGINEERING

**Two seats independently, from the live ledger:**

> Current engine: **tracked-race 9.4%, n=32, Wilson [3.2%, 24.2%]** — the upper bound is half a
> coin flip. The blended 24.6% is carried by the **retired v1 engine** (41.4%, n=29).
>
> `tailCapture` — the engine's OWN served field: top-decile surges (≥15.5×), **n=13, raced 13,
> LED 0 (0.0%)**. All 15 LED wins come from BELOW the top decile. Median LED lead 11 days,
> inside the 14-day backdating window.

**The demonstrated lead is exactly zero in the fat part of the distribution.** An
arrival-probability framing averages a 100× surge and a 3× surge into one number reading 24.6%
where the magnitude-weighted read is 0%. The instrument exists and is already computed; what was
missing was the decision to report it (Economist).

**Buyer's Desk, the sentence required in the first ten minutes:** *"Our early-detection rate is
25.9% across 58 tracked races — Wilson 16.3% to 38.4%, entirely below the 50% random-order null —
and zero of our 15 wins has been corroborated by our independent referee."*

---

## 3. FIVE OF NINETEEN REGISTER ROWS HAVE ENFORCERS THAT CANNOT FAIL

The Forecaster broke the **claims**, not the enforcers, and ran the gate:

| Row | Claim broken | Caught? |
|---|---|---|
| `C-DMEASURED-SERVED` | deleted every executable `d_measured` line from all four surfaces, kept the comments | **NO** |
| `C-SEAL-RECOMPUTED` | claim says "editing any binding term turns the gate RED" — false | **NO** |
| `C-FIREWALL-GATE` / `C-TESTS-RUN` / `C-PAYLOAD-REBUILD` | every gate banner demoted to a comment | **NO** |
| `C-R1` / `C-R2` | payoff ledger de-listed / ABSENT returns a rate | caught |

**17 of 19 enforcers are existence checks.** `kind="lint"` is `ref in ("L1".."L4")` — a constant.
`kind="test"` is a file-exists + substring (six markers are two characters). `kind="hookgate"` is a
substring in the hook. **`19 of 19` prints BEFORE the four lints that could contradict it.**

---

## 4. THE SEAL WAS CIRCULAR — and this was a fix made the day before

The Forecaster attacked the recomputing `sealed` enforcer seven ways. **Six passed GREEN**, because
it read the expected digest **out of the document it was checking**:

- **E1** Part A substitution + republished digest → GREEN
- **E5b** entire document replaced by a stub reading *"D is retained permanently. No result at
  2027-02-28 demotes it."* → GREEN
- **E4** `row_sha256` → 64 zeros → GREEN (the PIT anchor was never read)

**FIXED THIS SESSION** (`PIT_SEAL_ANCHORS.md` cross-check; E1/E4/E5b now RED, replayed and
verified). **STILL OPEN: E2** — an amendment appended *below* the boundary passes, and t6 asserts
that as correct. The Forecaster: *"t6 is not a control on the control — it is the escape route,
asserted as correct."*

**And the Forecaster scored his own round-5 error first, unprompted:** the seal DOES reproduce; he
reported a property of his search as a property of the artifact, one day after the org codified
that exact error (*"0-of-15 is silence, not refutation"*). He also found **his own predictions were
never entered in `FORECAST_REGISTER.md`** — *"the seat that demands pre-registration is running a
file drawer."*

---

## 5. THE THREE FIXES THAT WERE WRONG WITHIN HOURS OF SHIPPING

1. **`corroboration_at_enroll` was not the §15a quantity** (Expansionist + Statistician,
   independently). No `_title_sig`, no tier filter, no time bound, no v2.1 spike vote — five
   divergences, all biasing upward; and it returned **0, not None**, on an empty set. *"A rating
   factor whose stamped definition is not the doctrine's, reintroduced inside the fix for it."*
   **→ DISABLED this session.**
2. **`unmeasured_fraction = 0.752` is a different population** (Statistician) — market-instrument
   component degeneracy, not D over ledger rows. Quoted in a docstring and twice in board packs;
   four seats reasoned from it. The real D-unmeasured rate **is computed by no query in this repo.**
   **→ RETRACTED this session.**
3. **The extraction recipe added to fix "unverifiable in practice" was itself mangled** (Guardian)
   — literal CR/LF bytes where escapes were meant. **→ REWRITTEN this session.**

---

## 6. WHAT THE SEATS AGREED ON — the diagnosis

**Guardian (the unifying insight):** *"It is a QUANTIFIER problem: a universally-quantified claim
enforced by a particular-instance check. Every enforcer that ENUMERATES its population has held.
Every enforcer that NAMES AN INSTANCE has been evaded — by the next instance, every time."*

**Forecaster (the specific next costume):** *"the enforcer that binds a value nobody produces."*
`REFLEXIVITY_AUDIENCE_TRIP` seals against an `audience_count` with **zero producers**;
`panel_displayed_at` appears only in the docstring promising it; `SERVED_FIELDS` is a list of one.

**Operator / Economist (the rate):** 36 commits + 2 nine-seat boards vs **3 resolved races, 0 wins**.
*"The remedy rate is now the leading cause of the defect rate."*

---

## 7. DECISION TABLE

| Item | Chal | Guard | Expan | Buyer | Exec | Econ | Oper | Stat | Fore |
|---|---|---|---|---|---|---|---|---|---|
| Class fixed? | REJ | **EXTENDED** | — | — | REJ | REJ | REJ | — | **5 of 19 rows** |
| Negative controls "proven to fire" | A-W-C (8 rows have none) | — | — | — | — | — | — | — | — |
| `corroboration_at_enroll` | REJ | — | **REJ** | — | — | — | — | **REJ** | — |
| `category_at_enroll` | — | — | **REJ** (460 terms, 0 non-Latin) | A-W-C | — | — | — | — | — |
| tri-state 4c | REJ (1 of 4 paths) | REJ (2 surfaces) | A-W-C | — | REJ | — | — | REJ (score unchanged) | — |
| `panel_invariants` R1 | — | A-W-C (0 callers) | A-W-C | — | **KEEP** | A-W-C | hygiene | — | — |
| R2 reflexivity | — | A-W-C (measurement unbuilt) | — | — | **SPECULATIVE** | **REJ** | CUT | — | **REJ as prereg** |
| L4 generalises? | **NO** | **NO** | — | **NO** | **NO** | — | — | — | **NO** |
| `Payload-Rebuild-Pending` | REJ as control | — | — | A-W-C + expiry | **not a control** | A-W-C | zero enforcement | — | A-W-C |
| PII `author_history` | — | — | — | **REJ — INSTANT KILL** | — | — | — | — | — |
| Deploy state | REJ | — | — | NOT DONE | **0 in production** | — | **11 unpushed** | — | — |
| The panel | CUT | CUT | CUT 2 quarters | — | CUT | CUT | CUT | CUT | CUT |

**Unanimous CUT: the Base Rate Panel.** **Unanimous: the next convening must be a DATA event.**

---

## 8. FIXED THIS SESSION, EACH VERIFIED BY REPLAYING THE ATTACK

| Fix | Proof |
|---|---|
| `REFLEXIVITY_AUDIENCE_TRIP` 5001 → 250 | suite 12→13 passing |
| `corroboration_at_enroll` disabled | returns None; old impl retained unreachable |
| `unmeasured_fraction 0.752` retracted | docstring corrected, population named |
| Seal extraction recipe | rewritten in prose + Python |
| **PII_POLICY §2** | now states `author_history` IS a person-level record, with three gaps named |
| **Seal enforcer anchored** | E1/E4/E5b replayed → all RED |
| 12 commits pushed | `origin/main == HEAD` |

**NOT deployed. Deliberately** — the Executioner's instruction: 4c covers 1 of 4 paths, and a
partial deploy makes one field honest while `rich` beside it stays false.

---

## 9. WHAT THE SEATS SAY IS NEXT — the convergent ordering

1. **Complete 4c across all four paths, then deploy** (`_format_score_rows`, `calibration_engine`,
   `Screener.tsx`). Probe the **NULL stratum**, not a convenient row.
2. **PII erasure path + retention** — Buyer's Desk: procurement stops here.
3. **Make the register capable of failing** — count after lints; `test`/`lint`/`hookgate` execute.
4. **CI on hardware the author does not own.**
5. **`resolution_mode`** — the accuracy denominator is still a function of the Heroku bill, and the
   guarded sweep stamps `last_checked` on rows it never fetched (Operator, live data defect).
6. **Magnitude-stratify every published rate** (`tailCapture` out of a field and into the standing
   arithmetic).
7. **Re-resolve the ledger with `detection_date` = first floor-crossing** — Challenger: backdating
   adds up to 14 days of lead to every row and the median LED lead is 11.

**CUT, unanimously:** the panel. **CUT:** the calendar-convened board.

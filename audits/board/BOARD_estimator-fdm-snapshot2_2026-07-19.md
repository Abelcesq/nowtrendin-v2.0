# BOARD SESSION 2026-07-19 — R1 estimator un-hold · R2 final_del_mundial · R3 snapshot #2 gate
**COMPLETE (6/6 memos).** Supersedes the .PARTIAL record (session-limit interruption; Guardian +
Challenger carried over verbatim from it). Evidence pack: scratchpad
EVIDENCE_PACK_board3_2026-07-19.md. Every archetype made independent live pulls; all pack
numbers survived every re-pull. **Chairman rules per item; nothing here is a decision.**

## HEADLINE RESULTS

**R1 — UN-HOLD RECOMMENDED 6/6** (unanimous), all six strictly **into the build + backtest
phase**: the serve flip stays founder-gated behind pre-registered hard gates. The mechanism-
proof condition of the original hold is met (49/50 replay + britain caught by the purpose-built
shadow check + final_del_mundial as a genuine out-of-sample landing).

**Prediction-status correction (Executioner, live-verified):** 3 of the 4 registered 07-19
predictions are **VOIDED, not pending** — mamdani (m 16→32), taco (m 17→52, plat 9→35) and
cyclosporiasis_outbreak (m 10→19) show RENEWED EXPANSION, which was the predictions' explicit
escape clause; they must not be scored as failures OR successes. final_del_mundial (mentions
falling, no renewal) cliffed at exactly its 7th high cycle — the clean out-of-sample win.
Mechanism standing: 50/51.

**NEW ARTIFACT found during the session (Executioner):** the served embedded history contains
**duplicate cycle rows** (07-19T03 twice on ≥3 topics). The baseline query is LIMIT 12 by rows,
so duplicates shorten the window in time and can transiently move the median — the prime
suspect for britain's one-cycle w collapse (its magnitude was CONSTANT at 54.49 through the
dip, so the BASELINE moved, not the inputs). A read-only dedup diagnostic is the new step 1.

**R2 — OBSERVATION VERIFIED / FINDING REFUTED AS WRITTEN, 6/6.** The frozen quantity is the
blend-INPUT stack, by design: `attention_magnitude` is a PEAK-HOLD statistic (max mainstream
log-engagement over the 72h window — one ~21k-view signal reigned 8 cycles ⇒ 36.8 byte-
identical), breadth saturates at ≥4 platforms (12→10 invisible), M/I/P static, w pinned 1.0.
Arithmetic check (Executioner): 36.8·0.55 + 28 + 14.35 = 62.59 exactly. Raw mention counts are
NOT an argument of the w=1.0 formula — the check graded the score against a non-input. "Cached
serve / INV-1" refuted by the check's own sibling fields varying. The cliff was the confirmed
mechanism on schedule. Disposition 6/6: CLOSE as mechanism instance; reclass off
HIGH·SCORE_AFFECTING; one OPERATIONAL assessor fix (below). Challenger keeps one residue: a
component pull confirming M/I/P are genuinely pinned to ±0.01 (one read-only query).

**R3 — INSTRUMENT CONDUCT APPROVED 6/6 (with conditions).** v2.1.1's first live run honored
the rebuild: queue 21→2; honest N/A grades at tiny n; referee 19/20 with truthful cause codes;
Wilson interval + 0-FP disclosure printed; epoch table reconciles (67+20=87) with per-epoch
tracked-race; drift series started (rho=0.142 — low = thesis-right); E-check's INSUFFICIENT on
an all-ephemera day was verified correct against the fresh trending list. britain = the new
shadow check catching exactly its design target on day one (model conduct).

## THE SIX MEMOS (verdicts + unique contributions; full texts in the session transcript)

- **GUARDIAN** (R1 AWC / R2 refuted / R3 approve+1): the moat lives on the RISE side — burden
  of proof is asymmetric; interpolated quantile primary, EMA REJECT (Goodhart knob +
  principle drift); hysteresis asymmetric ONLY (up-crossing untouched); referee chastity
  (Wikipedia must never become a tuning target); no bundling; alarm-blindness watch (each
  encoded check-null must cite its board ruling).
- **CHALLENGER** (R1 AWC-backtest-only / R2 partially refuted / R3 AWC): the 49/50 is partly
  tautological (w moves whenever det moves ≥30 by arithmetic) — require the median-catchup
  branch scored SEPARATELY (~80%+); single-regime warning; add a plateau-hours-NOT-up gate so
  hysteresis can't pass by institutionalizing stale highs; sorted[len//2] upper-middle bias
  named; B.volatility vs B.asymmetry gave OPPOSITE verdicts on the same series (flip-gate the
  "burst dynamics" label); register the 30-pt step floor + 10k traffic floor provenance.
- **EXPANSIONIST** (R1 AWC / R2 refuted / R3 AWC): flap = product-integration defect (alert-
  driven enterprise clients get paged on BREAKOUT↔MONITORING round trips); NO IN-MEMORY
  ESTIMATOR STATE (derive hysteresis regime from the previous STORED row — the warm/cold-map
  lesson, fleet-consistency); window must be specced in TIME not cycles (12 rows secretly
  encodes the cron cadence); `_BREADTH_FULL=4` saturation decays into a no-op as the source
  roster grows (named scale-debt); per-language referee panel (es.wikipedia for
  final_del_mundial), never en-only.
- **OUTSIDER** (R1 AWC / R2 refuted / R3 AWC): the plateau is the exposure, not the cliff —
  the stale-plateau-hours chart is the one to show a client; SYMMETRIC deadband only (rejects
  asymmetric as plateau-extension — see disagreement 2); zero-delayed-crossings as an absolute;
  the LED corroboration status must print wherever 24.2% appears [session note: the re-run WAS
  done 2026-07-18 — 7/8 resolve, stored 0s are honest refutations; the open ask is the
  DISCLOSURE, not the re-run]; v2-epoch 7.7% (n=13) must always carry its young-races
  disclosure; final_del_mundial entered mainstream 2 days AFTER the final — have the
  one-paragraph answer ready before any World Cup demo.
- **ECONOMIST** (R1 AWC / R2 refuted-close / R3 AWC): quantile/order-statistic family, never
  EMA (a tax on the rare huge surges; imposed decay template); THE HYSTERESIS RESOLUTION —
  asymmetric **re-crossing-only Schmitt trigger with refractory scope**: first upward crossing
  untouched (lead), ordinary downward release untouched (revulsion is fast), only a RE-crossing
  within k cycles of a release needs threshold+h — satisfies both the Guardian's and the
  Outsider's constraints simultaneously; h MEASURED from fleet dispersion, registered a
  priori; window-length sensitivity reported but not changed; formula-arguments null for
  freeze checks; keep printing the ugly 7.7% with its context.
- **EXECUTIONER** (R1 SHIP staged / R2 refuted-close / R3 verified + v2.1.2 patch): build-ready
  spec — **asymmetric slew-limited median** (baseline_t = baseline_{t-1} + clip(median−prev,
  −∞, +up_rate); up slow, down instant; first-crossing bit-identical by construction) with the
  technical counter to pure quantiles: the magnitude series is near-binary (peak-hold), so any
  pure quantile still steps in half-gap jumps; DEDUP FIRST (in the query, never deleting rows),
  hysteresis only if residual, hold-last-w FORBIDDEN; reproduction gate (harness must match
  stored w on ≥95% of cycles before any candidate is scored); G1–G5 gates incl. cliff bound
  (max baseline-driven single-cycle |Δdet| ~79 → ≤20 registered); DUAL_BASELINE_V2 shadow mode
  3–5 days before any founder flip; assessor must CONSUME board rulings (known-mechanism table
  keyed by ruling_id) so confirmed signatures re-emit LOW-tracked, not page-one HIGH.

## DISAGREEMENTS (signal, not noise)

1. **Estimator family**: interpolated rolling quantile (Guardian, Challenger, Expansionist,
   Outsider, Economist) vs slew-limited median-target (Executioner, with the bimodal peak-hold
   arithmetic argument that pure quantiles still step on OUR data). Resolution path: both are
   candidates in the SAME pre-registered sweep; the reproduction gate + G1–G5 pick the winner
   empirically. No memo objects to running both.
2. **Hysteresis symmetry**: Outsider (symmetric deadband only; asymmetric = plateau extension)
   vs Guardian/Expansionist/Executioner (asymmetric; up-crossing untouchable). The Economist's
   re-crossing-only refractory design dissolves the conflict: it delays NEITHER first
   up-crossings NOR ordinary down-releases — only rapid re-crossings after a release. Both
   camps' stated constraints are satisfied; recommend it as the default hysteresis candidate.
3. **How much the replay proves**: Challenger (locus proven, cause-share unproven — score the
   median-catchup branch separately) vs the rest (confirmed). Costless to resolve: one re-score
   of the existing replay output.
4. **What to do about peak-hold magnitude**: all six agree it is defensible design and must NOT
   ride R1; Guardian/Expansionist/Executioner flag it as a future separate proposal;
   Economist defends it outright (the max IS the information in Extremistan).

## ITEM-15 RESEARCH RESULTS (held-out, read-only; reports in audits/ledger-research/)

- **15a breadth-priority** (BREADTH_PRIORITY_BACKTEST_2026-07-19.md): the mining doc's M=80-vs-50
  LED separation DOES NOT REPLICATE (win-rate dead tie 28.6%/28.6%). Robust effect:
  **enrollment efficiency** — top-half breadth slots are 60% real races vs 19% (~3× races per
  slot at flat win rate); one genuine low-breadth LED counterexample (socialists). Reframed as
  an enrollment-ORDERING tiebreaker; gate on a prospective alternate-slot A/B.
- **15b fast-lane recheck** (FASTLANE_RECHECK_DESIGN_2026-07-19.md): honesty correction —
  **verdicts CANNOT be flipped by faster validation** (detection_date and breakout_date are
  both fixed before any sweep); the 1–2-day "losses" were real (referee confirms 4/5). Re-scoped
  as a $0 resolution-latency + match-validity lane (8/9 covered rows surfaced 2–20d earlier;
  referee CONTRADICTS 4/9 measurable near-miss verdicts — match-validity metadata is the real
  deliverable). GHOST_FEEDS remains the hit-rate lever.
- **15c positioning floor-pin** (POSITIONING_FLOORPIN_RESEARCH_2026-07-19.md): §17 violation
  measured live — 281/300 instruments render a score-EXCLUDED Insider Tracking 0.0; 8 pinned at
  30.0 (z=0, no evidence); all 12 crypto coins serve pinned D=30.0 as if measured; mobile
  consumes no data_coverage at all (parity gap). Un-bundle: 4 display-only §17 fixes
  (no backtest needed) + 1 backtest-gated score-side exclusion (~173 rows + crypto).

## DECISION TABLE FOR THE CHAIRMAN

| # | Item | Class | Board standing |
|---|---|---|---|
| D1 | UN-HOLD R1 into build+backtest: dedup diagnostic → harness + reproduction gate → pre-registered sweep (both estimator families; Economist re-crossing hysteresis default) → gate table (G1–G5 + plateau-hours + median-catchup re-score) → founder review → shadow mode → founder flip | SCORE_AFFECTING (flip); build phase OPERATIONAL | 6/6 |
| D2 | Duplicate-cycle-rows dedup diagnostic (read-only, fleet-wide sizing; britain glitch confirm/refute) | OPERATIONAL | Executioner; none oppose |
| D3 | Close R2 as mechanism instance; reclass off HIGH·SCORE_AFFECTING; one component pull (M/I/P pinned?) | OPERATIONAL | 6/6 |
| D4 | Assessor v2.1.2 patch: B.freeze formula-arguments null + known-mechanism ruling table + dup-hygiene check + flip-gate the "burst dynamics" label + register step/traffic floor provenance + full-corpus E scan | OPERATIONAL (param bump) | 6/6 |
| D5 | Snapshot #2 → BOARD_VERIFIED (item 1 refuted/reclassed; item 2 verified-held into R1) | OPERATIONAL | 6/6 |
| D6 | Disclosure duo: LED-corroboration status printed wherever tracked-race appears; v2-epoch young-races note always paired | OPERATIONAL (display) | Outsider + Economist |
| D7 | 15c display-only §17 quartet (absent serialization, degenerate-baseline stamp, crypto coverage fields, mobile parity) | OPERATIONAL (display) | research-verified |
| D8 | 15c score-side degenerate-component exclusion | SCORE_AFFECTING (backtest first) | research-verified, gated |
| D9 | 15a enrollment-ordering tiebreaker → prospective alternate-slot A/B design | Measurement-side; board gate after A/B | reframed |
| D10 | 15b fast-lane as $0 latency + match-validity lane (never a judge; flag-gated) | OPERATIONAL (held-out) | re-scoped |
| D11 | Scale-debt log adds: `_BREADTH_FULL=4` saturation decay; window specced in time; no in-memory estimator state (standing constraint) | log/design constraints | Expansionist |

**Chairman — your decision per item.**

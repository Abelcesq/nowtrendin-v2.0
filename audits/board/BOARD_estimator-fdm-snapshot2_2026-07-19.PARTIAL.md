# BOARD SESSION 2026-07-19 — R1 estimator un-hold · R2 final_del_mundial · R3 snapshot #2 gate
# STATUS: PARTIAL — 2 of 6 memos completed before the Claude session usage limit interrupted
# the fan-out (Guardian + Challenger below, complete). Expansionist, Outsider, Executioner,
# Economist + the three item-15 research agents were terminated pre-output and will be
# relaunched after the limit resets (~11pm PT). Evidence pack: scratchpad
# EVIDENCE_PACK_board3_2026-07-19.md (fresh per-cycle final_del_mundial + britain data,
# WC-final pageviews, live ledger). Do not treat this as a completed board session.

## KEY CONVERGENCES OF THE TWO COMPLETED MEMOS (preview only — not a quorum)
- R1: both recommend un-holding ONLY into the backtest build, not to ship; both prefer an
  INTERPOLATED ROLLING QUANTILE/median over the same 12-cycle window; both REJECT an EMA
  baseline (Goodhart knob; recurrence blindness); hysteresis only as an asymmetric/deadband
  reversal guard with the UP-crossing untouched (enrollment-timing invariance is the hard gate).
- Challenger's material caveats: the replay's 49/50 partially tautological (w moves whenever
  det moves ≥30 by arithmetic — require the median-catchup branch scored SEPARATELY, ~80%+);
  the 4 REGISTERED 07-19T10/T16 predictions were 0-of-4 checked at review time — WAIT for them
  before announcing confirmation (final_del_mundial's on-schedule overnight cliff is consistent
  but post-hoc); single-regime sample warning; add a plateau-hours-NOT-up gate so hysteresis
  can't pass by institutionalizing stale highs; note sorted[len//2] upper-middle bias.
- R2: both VERIFY the observation, REFUTE the finding as written — the frozen quantity is the
  PEAK-HOLD attention_magnitude input (max-over-window by design; 36.8 byte-identical while
  the peak row stays in the 72h window) + breadth saturated (min(1,(n-1)/3)=1 for n>=4), and
  the check graded the score against mentions, which the w=1.0 pathway does not consume.
  Cache/INV-1 hypothesis refuted by the check's own sibling fields varying. Cliff = the
  confirmed mechanism on schedule (7th cycle). Follow-ups: OPERATIONAL only — B.freeze must
  read the served blend inputs + encode a saturation-aware null; Challenger keeps ONE open
  residue (M/I/P identical to +-0.01 across the frozen window needs a component pull to
  confirm they are genuinely pinned). Not a standalone SCORE_AFFECTING remedy.
- R3: both APPROVE the v2.1.1 instrument's conduct (honest abstentions, referee 19/20, epoch
  reconciled, Wilson+0-FP disclosures, drift baseline started; britain = the new shadow check
  catching exactly its target on day one). Challenger's conditions for snapshot #3: the
  B.volatility "burst dynamics" label must be flip-gated (same series got opposite verdicts
  from B.volatility vs B.asymmetry — rag/fifa/vegas_airport V-shapes PASSed as "monotone");
  register the 30-pt step floor + 10k traffic floor provenance; full-corpus E scan (2000/3353);
  dedupe same-hour cycle rows; always pair pooled 24.2% with the per-epoch split (v2: 7.7% n=13).

## MEMO — FIRST-PRINCIPLES GUARDIAN (complete, condensed only for length)
[Verdicts: R1 APPROVE-WITH-CONDITIONS un-hold (5-step gauntlet; enrollment-crossing invariance
zero-tolerance; interpolated quantile primary; EMA REJECT; asymmetric hysteresis only; forbid
estimator-vs-referee tuning, forbid bundling with any other change); R2 observation VERIFIED /
finding REFUTED (peak-hold by design; close-as-mechanism-instance + OPERATIONAL check fix; the
check's null must never pressure manufactured variance); R3 APPROVE conduct + one defect (B.freeze
ignored the blend-input fields the history was expanded to serve for its benefit); standing
warning re-affirmed: the Wikipedia referee must never become a tuning target; watch alarm-
blindness as checks accumulate nulls — each encoded null must cite its board ruling.]

## MEMO — THE CHALLENGER (complete, condensed only for length)
[Verdicts: R1 APPROVE-WITH-CONDITIONS — un-hold FOR THE BACKTEST BUILD ONLY, ship decision
returns to the Chairman post-backtest with pre-registered gates (regime-stratified replay; flap
count down AND plateau-hours not up; per-topic enrollment-crossing unchanged in EITHER direction;
no inflation; Taleb <=2-cycle gate; param bump + epoch stamp on ship). Would move to full
approve: the 4 registered predictions landing + median-catchup branch >=80% standalone. R2
observation VERIFIED / cache-branch REFUTED / one bounded OPERATIONAL component diagnostic /
B.freeze will structurally re-fire on every post-peak mainstream window without a saturation-
aware null. R3 APPROVE-WITH-CONDITIONS as summarized above.]

(Full verbatim memos preserved in the session transcript; this file collates for the record and
will be superseded by the COMPLETE 6-memo collation after the relaunch.)
